#lang sicp

(define double

(define (fast-mul a b)
  (define (iter m a b)
    (cond ((= b 1) m)
          ((= (remainder b 2) 0) (iter m (double a) (halve b)))
          (else (iter (+ m a) (double a) (halve (- b 1))))))
  (iter 0 a b))

(fast-expr 2 10)
(fast-expr 3 10)
(fast-expr 2 10000)